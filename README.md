# Book/Novel Generator!
Setup your .env (cp .env.example .env && nano .env) and run 'run_local.sh'.
You will need a Kokoro endpoint for TTS. 
Require ffmpeg installed on your system, in path.
Add --show-help for more info.
